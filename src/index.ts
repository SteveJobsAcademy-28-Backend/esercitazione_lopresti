import { printUserInfo, defaultUser, createUser } from "./models/utils/functions";
import { Guest } from "./models/User";
import { admin, SuperAdmin } from "./models/Admin";
import { guest, Admin, Developer } from "./models/utils/types";

console.log(printUserInfo);
console.log(defaultUser);
